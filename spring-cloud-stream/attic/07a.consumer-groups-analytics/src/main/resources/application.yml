spring:
  cloud:
    stream:
      bindings:
        transactionConsumer-in-0:
          destination: credit-card-transactions
          group: analytics
          consumer:
            partitioned: true
            instance-count: 10
            instance-index: 6
      kafka:
        bindings:
          transactionConsumer-in-0:
            consumer:
              auto-rebalance-enabled: false

server:
  port: 0

logging:
  pattern:
    console: "[%blue(%18thread)] %date{HH:mm.ss:SSS} [%cyan(%45logger{45})] %highlight(%-5level) %yellow(%X) %msg%n"
